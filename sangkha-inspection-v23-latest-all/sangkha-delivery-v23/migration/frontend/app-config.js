/* Change only this URL if your Worker name or account subdomain changes. Never put API keys here. */
window.SANGKHA_CONFIG = Object.freeze({
  API_BASE_URL: "https://sangkha-inspection-api.25071.workers.dev"
});
